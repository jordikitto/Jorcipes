# Update 3  
* Rename ControlledAPIClient to MockAPIClient? Or Spy?  
* Use a key enum or static let for the user defaults key  
* For folders with more than 4 views in them, use subfolders.  
* In tests, replace `try? await Task.sleep()` with `try await Task.sleep()`, and make the test throwing.  
* Rename JorcipesTestSupport -> JorcipesNetworkingTestSupport  
